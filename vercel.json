{
  "framework": "vite",
  "installCommand": "npm ci --legacy-peer-deps --no-audit --no-fund --ignore-scripts",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
